require 'spec_helper'

describe YmlCatalog do
  it 'has a version number' do
    expect(YmlCatalog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
